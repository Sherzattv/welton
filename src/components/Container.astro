---
// Центрирующий контейнер с боковыми отступами.
interface Props {
  class?: string;
  as?: keyof HTMLElementTagNameMap;
}
const { class: className = "", as = "div" } = Astro.props;
const Tag = as;
---

<Tag class={`mx-auto w-full max-w-[1680px] px-gutter ${className}`}>
  <slot />
</Tag>
