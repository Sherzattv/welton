---
// Центрирующий контейнер с боковыми отступами.
interface Props {
  class?: string;
  as?: keyof HTMLElementTagNameMap;
}
const { class: className = "", as = "div" } = Astro.props;
const Tag = as;
---

<Tag class={`mx-auto w-full max-w-[1460px] px-5 sm:px-6 lg:px-8 ${className}`}>
  <slot />
</Tag>
