---
import Container from "../Container.astro";
import GridLines from "../GridLines.astro";
import Heading from "../Heading.astro";
import Button from "../Button.astro";
import { contact } from "../../data/home";
---

<section id="invoice" class="relative flex min-h-screen flex-col justify-between overflow-hidden py-16 text-paper sm:py-20">
  <img
    src="/img/high-rise-buildings-.jpg"
    alt=""
    loading="lazy"
    class="absolute inset-0 h-full w-full object-cover"
  />
  <div class="absolute inset-0 bg-charcoal/55"></div>
  <GridLines />

  <Container class="relative z-10">
    <Heading as="h2" size="lg" class="max-w-2xl text-paper" data-split>
      Предварительный расчёт остекления под ваш объект
    </Heading>
    <div class="mt-8" data-reveal>
      <Button variant="light" href="#contacts">Оставить заявку</Button>
    </div>
  </Container>

  <Container class="relative z-10">
    <p data-reveal class="text-paper/80">Не хотите ждать звонка?<br />Позвоните нам по этому номеру:</p>
    <a
      data-reveal
      href={contact.phoneHref}
      class="display mt-4 block text-4xl text-paper transition-colors hover:text-accent sm:text-6xl"
    >
      {contact.phone}
    </a>
  </Container>
</section>
