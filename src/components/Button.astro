---
// Фирменная кнопка: подпись + вертикальный разделитель + стрелка.
// Острые углы, ховер уводит в зелёный акцент, стрелка едет вправо.
interface Props {
  href?: string;
  variant?: "light" | "accent" | "outline";
  class?: string;
  type?: "button" | "submit";
  [key: `data-${string}`]: string | boolean | undefined;
}
const {
  href,
  variant = "light",
  class: className = "",
  type = "button",
  ...rest
} = Astro.props;
const Tag = href ? "a" : "button";

const base =
  "group inline-flex items-stretch justify-between overflow-hidden rounded-[3px] text-[14px] font-medium tracking-[-0.6px] transition-colors duration-300 ease-out";
const variants = {
  light: "bg-paper text-charcoal hover:bg-accent hover:text-paper",
  accent: "bg-accent text-paper hover:bg-accent-hover",
  outline:
    "border border-paper/30 text-paper hover:border-accent hover:bg-accent hover:text-paper",
};
---

<Tag
  href={href}
  type={href ? undefined : type}
  class={`${base} ${variants[variant]} ${className}`}
  {...rest}
>
  <span class="flex h-full items-center pr-3 pl-[clamp(16px,1.333vw,19.2px)] leading-[21.7px]"><slot /></span>
  <span
    class="flex h-full w-[51px] items-center justify-center border-l border-current/15 transition-colors duration-300"
  >
    <svg
      width="18"
      height="14"
      viewBox="0 0 18 14"
      fill="none"
      aria-hidden="true"
      class="transition-transform duration-300 ease-out group-hover:translate-x-1"
    >
      <path
        d="M11 1l6 6-6 6M17 7H1"
        stroke="currentColor"
        stroke-width="1.6"
        stroke-linecap="square"
      ></path>
    </svg>
  </span>
</Tag>
